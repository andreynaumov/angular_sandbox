import { Autocomplete } from './autocomplete';

describe('Autocomplete', () => {
  it('should create an instance', () => {
    const directive = new Autocomplete();
    expect(directive).toBeTruthy();
  });
});
